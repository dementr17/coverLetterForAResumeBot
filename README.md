# Telegram Bot для создания шаблонов сопроводительных писем

Бот для Telegram, который автоматически создает персонализированные шаблоны сопроводительных писем на английском языке на основе резюме пользователя.

## 🚀 Возможности

- 📄 Принимает резюме в виде текста или файла (PDF, DOCX, TXT)
- 🤖 Генерирует шаблон сопроводительного письма на английском языке
- 📝 Создает шаблон с плейсхолдерами в квадратных скобках `[ ]` для последующей замены
- 🔔 Отправляет уведомления администратору о критических ошибках
- 💬 Человеко-ориентированный интерфейс с понятными сообщениями

## 📋 Требования

- Python 3.8 или выше
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- OpenAI API ключ (получить на [platform.openai.com](https://platform.openai.com))

## 🔧 Установка

### 1. Клонирование репозитория

```bash
git clone git@github.com:dementr17/coverLetterForAResumeBot.git
cd coverLetterForAResumeBot
```

### 2. Создание виртуального окружения (рекомендуется)

Рекомендуется использовать виртуальное окружение для изоляции зависимостей проекта:

```bash
# Убедитесь, что используете Python 3
python3 --version

# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте виртуальное окружение
# На macOS/Linux:
source venv/bin/activate

# На Windows:
# venv\Scripts\activate
```

После активации виртуального окружения в начале строки терминала появится `(venv)`.

### 3. Установка зависимостей

```bash
# Убедитесь, что виртуальное окружение активировано
# (должно быть видно (venv) в начале строки)

# Обновите pip (рекомендуется)
pip install --upgrade pip

# Установите зависимости
pip install -r requirements.txt
```

**Примечание:** Если вы не используете виртуальное окружение, установите зависимости глобально:

```bash
python3 -m pip install --upgrade pip
python3 -m pip install -r requirements.txt
```

### 4. Настройка конфигурации

Создайте файл `secrets.py` в корне проекта:

```python
# Private tokens and secrets
# DO NOT commit this file to git!

# Telegram Bot Token
BOT_TOKEN = "ваш_telegram_bot_token"

# OpenAI API Token
CHATGPT_TOKEN = "ваш_openai_api_key"
```

**Важно:** Файл `secrets.py` уже добавлен в `.gitignore` и не будет закоммичен в репозиторий.

### 5. Настройка промпта

Файл `promt.txt` содержит шаблон для генерации сопроводительных писем. Вы можете изменить его по своему усмотрению.

## ▶️ Запуск бота

### Локальный запуск

**Если используете виртуальное окружение:**

```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate

# Запустите бота
python3 bot.py
```

**Если не используете виртуальное окружение:**

```bash
python3 bot.py
```

Бот запустится и начнет обрабатывать сообщения. Вы увидите в консоли сообщение "Бот запущен...".

### Запуск в фоновом режиме (Linux/Mac)

**С виртуальным окружением:**

```bash
# Активируйте виртуальное окружение и запустите в фоне
source venv/bin/activate && nohup python3 bot.py > bot.log 2>&1 &
```

**Или используйте `screen` или `tmux`:**

```bash
screen -S telegram_bot
source venv/bin/activate
python3 bot.py
# Нажмите Ctrl+A, затем D для отсоединения
```

**Без виртуального окружения:**

```bash
nohup python3 bot.py > bot.log 2>&1 &
```

### Остановка бота

Если бот запущен в обычном режиме, нажмите `Ctrl+C` в терминале.

Если бот запущен в фоне, найдите процесс и завершите его:

```bash
# Найти процесс
ps aux | grep "python3 bot.py"

# Завершить процесс
pkill -f "python3 bot.py"
```

## 📱 Использование

1. Найдите вашего бота в Telegram (по имени, которое вы указали при создании у @BotFather)
2. Отправьте команду `/start` для начала работы
3. Отправьте резюме одним из способов:
   - **Текстом**: просто скопируйте и отправьте текст резюме
   - **Файлом**: отправьте файл в формате PDF, DOCX или TXT
4. Бот автоматически создаст шаблон сопроводительного письма
5. Замените плейсхолдеры в квадратных скобках `[ ]` на данные конкретной вакансии

### Команды бота

- `/start` - Начать работу с ботом
- `/help` - Получить справку по использованию

## 🔔 Уведомления об ошибках

Бот автоматически отправляет уведомления администратору (ID указан в `bot.py`) о критических ошибках, особенно связанных с:
- Проблемами OpenAI API
- Ошибками аутентификации
- Ошибками обработки файлов
- Ошибками региона (если OpenAI API недоступен в вашем регионе)

## 🛠️ Структура проекта

```
coverLetterForAResumeBot/
├── bot.py              # Основной файл бота
├── promt.txt           # Промпт для генерации шаблонов
├── requirements.txt    # Зависимости Python
├── secrets.py          # Токены и секреты (не коммитится)
├── venv/               # Виртуальное окружение (создается при установке)
└── README.md           # Этот файл
```

**Примечание:** Папка `venv/` создается при создании виртуального окружения и не должна коммититься в репозиторий (уже в `.gitignore`).

## ⚠️ Решение проблем

### Ошибка "ModuleNotFoundError: No module named 'telegram'"

Установите зависимости:

**С виртуальным окружением:**
```bash
source venv/bin/activate
pip install -r requirements.txt
```

**Без виртуального окружения:**
```bash
python3 -m pip install -r requirements.txt
```

### Ошибка при установке зависимостей (PEP 668)

Если видите ошибку, связанную с PEP 668, используйте виртуальное окружение:

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Ошибка "SyntaxError: Non-ASCII character"

Убедитесь, что используете Python 3:
```bash
python3 bot.py
```

### Ошибка "unsupported_country_region_territory"

OpenAI API недоступен в вашем регионе. Решения:
- Используйте VPN
- Используйте прокси для API запросов
- Используйте API ключ из поддерживаемого региона

### Ошибка "Permission denied (publickey)" при клонировании

Добавьте SSH ключ в SSH агент:
```bash
ssh-add ~/.ssh/id_ed25519_github
```

## 📝 Зависимости

- `python-telegram-bot` - Библиотека для работы с Telegram Bot API
- `openai` - Библиотека для работы с OpenAI API
- `PyPDF2` - Для обработки PDF файлов
- `python-docx` - Для обработки DOCX файлов

## 🔒 Безопасность

- **Никогда не коммитьте** файл `secrets.py` в репозиторий
- Храните токены в безопасном месте
- Регулярно обновляйте зависимости для исправления уязвимостей
- Используйте переменные окружения для продакшн окружения

## 📄 Лицензия

Этот проект создан для личного использования.

## 👤 Автор

dementr17

## 🤝 Поддержка

При возникновении проблем создайте Issue в репозитории или обратитесь к администратору бота.

---

**Приятного использования! 🚀**

